resources:
- ../../../base/cluster
- ../../../tenants/tenant-a

commonAnnotations:
  configsync.gke.io/cluster-name-selector: pre-prod-cluster

patches:
- target:
    kind: RepoSync
    name: repo-sync
  patch: |-
    - op: replace
      path: /spec/git/branch
      value: pre-prod