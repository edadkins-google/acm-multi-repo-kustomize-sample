commonLabels:
  environment: prod

resources:
- prod-cluster-lon
- prod-cluster-ny

patches:
- target:
    kind: RepoSync
    name: repo-sync
  patch: |-
    - op: replace
      path: /spec/git/branch
      value: prod

